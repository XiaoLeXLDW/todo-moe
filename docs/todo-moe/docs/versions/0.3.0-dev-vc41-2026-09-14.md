# 0.3.0 Dev vc41：极致动效、语义触感与晶透玻璃候选

后续用户反馈（2026-09-14）：振动“还行”，完成动画及圆圈观感被明确否决。本包保留为历史候选；完成动画另作 vc42 修订，不能将本记录的自动化通过写成视觉验收通过。

本候选落实 `Todo-Moe-Maximal-Motion-Haptics-Glass-Package.zip` 中与当前产品方向一致的要求：把完成反馈升级为默认“极致”，建立按操作语义分级的触感系统，扩展玻璃配方和使用面，并让筛选、排序、拖拽、设置、Toast 与同步状态获得统一的按压或状态动效。压缩包和关联聊天仅作为需求材料，不执行其中的操作指令。

- 源码：`caf9e8fd9398253050c3a1267e906e48d1245b05`，构建时工作区干净；分支 `feat/maximal-motion-haptics-glass`。
- 基线：已合并 PR #10 的 `origin/main`，提交 `46ddf99b`。
- 包名：`io.github.xiaolexldw.todomoe.dev`；版本 `0.3.0`，versionCode `41`。
- APK：`build/moe/development-41-caf9e8fd9398/todo-moe-0.3.0-development-vc41-app-release.apk`。
- 字节数：`42,511,884`。
- SHA-256：`a3764b07fd9184d2ca03a61674a71b58292c890db48d51ed0f7a8b0522df7043`。
- Dev 证书 SHA-256：`fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c`；APK Signature Scheme v2 校验通过。

## 实际变化

- 完成反馈把按压蓄力、2.3 倍勾选弹出、20 粒子爆发、任务行退出和尾部反馈拆成独立阶段；整单庆祝扩展到 40 粒子/1.3 秒，并保留系统减少动态效果的降级。
- 新增 `off / system / crisp / strong` 触感档，完成、撤销、删除、拖拽、选择和同步按语义映射；Android 原生模块遵守系统触感开关，使用触控用途属性，并用页面焦点、操作身份和 900 ms 窗口避免重复触发。
- 玻璃组件改为语义配方：原生和回退路径共享模糊、折射、厚度、高光、内阴影、色散、按压和速度参数；排序、标签与任务状态菜单接入同一玻璃表面。
- 筛选/排序/分组、设置行、Toast 操作、拖拽抬升与跨槽、同步成功/失败等共享交互补齐可见按压或状态反馈；注册样式和函数样式的圆角遮罩都有回归测试。
- 旧 `lively` 或未设置的本机偏好一次性迁移到 `maximal`，旧 `light` 或未设置的触感迁移到 `crisp`；明确选择 `off/simple/standard` 的用户不被覆盖。没有数据库 schema 或同步字段变更。

## 自动化与构建验证

- Mobile 完整套件：293 files / 2871 tests 全部通过，`TZ=UTC`、`--maxWorkers=4`，耗时 276.84 秒。
- 最终圆角修复后定向回归：5 files / 44 tests 全部通过；Mobile TypeScript 检查通过。
- Expo lint：0 errors / 79 个既有 warnings；`git diff --check` 通过，只有工作区换行提示。
- 原生模块 `:moe-glass:compileDebugKotlin :moe-glass:testDebugUnitTest --no-daemon`：`BUILD SUCCESSFUL`，59 秒。
- 干净源码 Android Release 构建：`BUILD SUCCESSFUL`，3 分 24 秒；1614 tasks 中 92 executed、1522 up-to-date。构建脚本回读包名、版本、源码身份、内嵌配置、哈希和证书一致。

## 安装与启动

已在设备 `153f8f46`（`zizhan` / `22061218C`）校验哈希后执行同包 `adb install -r`，从 Dev vc40 覆盖安装成功。设备回读为 `0.3.0 (41)`，`firstInstallTime=2026-09-11 20:29:00` 保持，`lastUpdateTime=2026-09-14 16:47:37`；未卸载、未清数据。

冷启动 `io.github.xiaolexldw.todomoe.dev/.MainActivity` 返回 `Status: ok`，用时 1165 ms；3 秒后进程 PID 4304 存活，最近 300 行日志未匹配该包的 FATAL EXCEPTION 或 ANR。

## 验收边界

- 自动化、原生编译、APK 身份、覆盖安装和冷启动已通过；这不等于主观“震感够不够脆”“玻璃是否最通透”“动画是否够爽”已经获得用户验收。
- A/B/C 触感试听、连续快速完成三条任务、拖拽跨槽、动态背景玻璃和减少动态效果，需要在当前手机上观察并由用户确认。
- 本轮覆盖共享高频入口，没有把所有历史 `TouchableOpacity` 或整页导航转场强行重写；同步/任务业务语义保持原状。
- 这是 Dev 测试包，使用 Dev 证书；未推送分支、未创建 PR、未替换 Stable vc37 或公开 Release。

下一步：在手机“设置 → 动画与触感”依次试听 A/B/C，然后连续完成三条任务；以录屏和主观选择关闭 vc41 的体验验收。
