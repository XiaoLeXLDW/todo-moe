# Dev vc11：增量构建系统入口去重 · 2026-09-12

状态：干净源码工程样本已构建，入口去重验证通过，但APK组件比对发现另一渠道的旧Widget注册残留，本版不作为交付候选；未安装、未发布。

本版修复多次prebuild后重复生成深链、分享和Context入口的问题，保留所有不同语义的过滤器。主任务在旧vc7实际APK和后续生成XML均取得RED，新回归调用真实插件周期；[根因、边界与原始证据](../../NATIVE-INTENTS-20260912.md)。

源码`11a58f98fa6745d23d7ef77fff2df63ccd2916cb`，dirty=false。实际Dev→Dev→Stable→Stable→Dev→Dev六次prebuild通过：MainActivity始终7组、Context始终2组，无完全相同的过滤器或节点；相同渠道的XML解析结果保持不变。日志和每次XML保存在`evidence/development/intent-prebuild-11a58f98/`。

Dev vc11实际构建4m37，APK 42,003,748字节，SHA-256 `a5436b6d275844b0e2bb87adf79598130b30c71980030a8de8c6012fbbf402be`。源码ZIP SHA-256 `a012443bbe3cf6969b8ebe270c17dbfb5b273c97a1ebdf91f7f4de822c7908c6`。本Dev包名、原测试证书、嵌入配置、16KiB zipalign、32个ELF库对齐及诊断标记移除均通过；这些单项没有覆盖其他渠道组件残留。

实际二进制Manifest中没有完全重复的intent-filter，但包含额外的`io.github.xiaolexldw.todomoe.widget.TasksWidget`，属于Stable身份的旧生成receiver。相同渠道内精确去重不能删除不同名称的组件，新增实际APK身份断言明确RED；日志`evidence/development/vc11-widget-channel-red.log`，判定`widget-channel-verification.json`的passed=false。`verification-summary.json`已补candidateAccepted=false，不覆盖先前已通过的窄项。新回归与既有native-brand测试5 pass，完整工程120 pass/1默认跳过/0 fail保持对应范围。

[本地工程样本](../../../../artifacts/0.1.0-dev-vc11/README.md)。下一候选需修复已知两个渠道间的旧Widget注册残留，并重新做真实插件、prebuild及APK验证；不把vc11的局部通过认作全部入口已通过。

手机仍为Dev vc7。诊断vc9待外屏解锁后复测现代文件选择器异常，之后才可升级到高版本干净候选。未清理手机数据或使用正式签名。
