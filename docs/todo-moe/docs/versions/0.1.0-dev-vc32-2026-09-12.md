# 0.1.0-dev / vc32 · 2026-09-12

本文件记录实际交付的 Dev APK。当前执行状态仍以 [IMPLEMENTATION](../../IMPLEMENTATION.md) 为唯一入口；本记录不代表 Stable 已签名或发布。

| 字段 | 记录 |
|---|---|
| 源码 | `9524df7cf7deb9e16d8fdc4b5701dc10cc8afd63`，dirty=false |
| 身份 | Todo Moe Dev / `io.github.xiaolexldw.todomoe.dev` / 0.1.0 / versionCode 32 |
| APK | `build/moe/development-32-9524df7cf7de/todo-moe-0.1.0-development-vc32-app-release.apk`，42,289,563 字节 |
| SHA-256 | `bf2da8b13c2fcd47c35bc3519a657edcb83d41d2544556cbbe33aff3b05df7bf` |
| Dev 证书 SHA-256 | `fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c` |
| 构建 | 3m02；包内配置核验通过，同证书 `adb install -r` 成功 |

## 本次变化

底栏使用固定来源的完整 SukiSU Compose 组件，保留组合背景、着色标签副本、原版透镜、高光和弹簧。原生模块接入 RN 页面背景，导航仍由原路由处理。vc31 首启时 Fabric 提前测量导致缺少 windowRecomposer；本包将 Compose 子视图测量放到挂载完成后，并修正玻璃模式切换后高光引用旧动画的问题。

原有完成/撤销、清单组织、新增面板与折叠配置修复均保留，不改变任务模型或同步 schema。

## 已获得的证据

- 模块编译和 9 项原生检查通过；JS 导航桥接 22 项测试、TypeScript 检查通过。固定来源的透镜与高光 shader 字符串已比较一致；这不等同于像素验收。
- MIX Fold 2 / Android 15 / API 35 外屏正常冷启动，UIAutomator 读到 `moe-sukisu-navigation`，确认实际使用新组件。
- 实测今天与收件箱来回拖动、中心反向拖动、按住外扩、镜内图标文字放大/着色/折射。首段录屏前两次拖动未触发；后续上沿来回录屏和单次中心反向操作成功，不能据此声称全部触摸情况通过。
- 新增面板打开后底栏不可访问，关闭后底栏恢复；应用进程保持。前一版 vc30 的内→外合屏草稿保留证据仍在总入口，新 Compose 底栏的折叠路径尚需验收。

私有设备证据位于 `evidence/development/device-20260911-153f8f46/`，包括 `353-vc32-compose-launch`、`compose-drag-vc32-repeat.mp4`、`355-vc32-upper-drag`、`356-vc32-center-reverse`、`357-vc32-capture-hide`、`358-vc32-returned-bar`。录屏包含个人工作区内容，不作为公开演示素材。

## 尚未完成

2026-09-13用户确认观感通过；内屏1914×2160深色布局、拖动切至收件箱、点击清单、新增面板隐藏/返回今天均实测通过（376—380设备记录）。已补测：透镜移动到收件箱后CANCEL回到今天；静止5秒2542→2542帧；后台3秒同进程CPU计数不变，恢复后原生底栏可用。内置沙盒的新增/完成撤销/玻璃演示约34秒，已退出并恢复个人工作区。旧GlassSurface性能记录不能代表新Compose底栏。当前无正式签名包、Stable安装验收或公开Release。

## Dev 转入 Stable 的数据准备

Dev 与 Stable 包名独立，Stable 不会自动读取 Dev 数据。正式包就绪后，先在 Dev 的“数据 → 备份 → 导出备份”导出，再在 Stable 恢复并核对任务、清单与分组；核对前保留 Dev 和导出文件。当前按用户选择关闭同步，本记录不要求配置云端。
