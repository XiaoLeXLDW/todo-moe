# 0.1.0 Stable / vc33 · 正式签名候选

正式签名与电脑端校验已完成。用户要求先完成不需要手机的工作，因此本版本尚未安装、迁移或真机验收，未公开发布。Dev vc32 的既有实测不代替本包的运行验收。

| 字段 | 记录 |
|---|---|
| 应用源码 | `c334a17366cb70993e69e7ec9391713c497aa35b`，dirty=false |
| 签名工作流源码 | `cee57bae6813ca8eecfff0a9aba6751a199c2fdb`，PR #5 已合并 |
| 身份 | Todo Moe / `io.github.xiaolexldw.todomoe` / 0.1.0 / versionCode 33 |
| APK | `artifacts/0.1.0-stable-vc33/signed/todo-moe-0.1.0-stable-vc33.apk` |
| 大小 | 42,436,914 字节 |
| APK SHA-256 | `697d13d4dae339291bb119b9d2efc52903e60162ae5e0b2abec1aa8f5a151ae8` |
| 正式证书 SHA-256 | `76cda5af5613e5e960036aa8041cb18c91475b297897ecec3cce04b4940c77e9` |
| 本次未签名输入 SHA-256 | `4d0ed4336ffd7e52f3926cf8b05ced1df9eb1016e432047ed0c954d9b45cf329` |
| 发布说明 SHA-256 | `035ab01d89df4ddb2514a375013505cfb4b197e68dc517ac0f8a40d74b389c12` |
| 签名产物 ID | `10303983313`，`todo-moe-signed-stable` |
| 产物 ZIP SHA-256（GitHub） | `57ce8e6d7f86d871be419526dfcc2ba1bb68260a6144616f7d4e7de5712628bb` |

## 已完成

- [PR #5](https://github.com/XiaoLeXLDW/todo-moe/pull/5) 的全部检查通过并合并；修复仅为签名命令的产物目录定位。
- [正式流程 34713022623](https://github.com/XiaoLeXLDW/todo-moe/actions/runs/34713022623) 的 preflight、综合检查、Dev 构建、Stable 构建、sign 均成功。
- 从该运行下载签名文件，独立核验 APK SHA-256、字节数、SHA256SUMS、发布说明哈希和 APK 内嵌源码/渠道/版本配置。
- Android 工具实测包名、应用名称和版本正确；唯一签名者为新建正式证书，RSA 4096，v2/v3 签名有效，16 KB ZIP 对齐通过。
- 签名后逐一核对未签名输入的 1665 个原始 ZIP 项，内容全部保留。校验结果存于 `artifacts/0.1.0-stable-vc33/signed/verification.json`。

## 失败及修复记录

首次运行 [34709028897](https://github.com/XiaoLeXLDW/todo-moe/actions/runs/34709028897) 在读取 `unsigned/build-manifest.json` 时失败，尚未执行密钥签名。`upload-artifact` 的 `build/moe/stable-*/*` 保留版本目录，而签名命令错误地假设下载后已扁平化。PR #5 改用批准的 versionCode 和 SHA 前 12 位定位确切目录，未修改应用代码、密钥或校验条件。实际下载产物的本地复现确认目录问题，13 项发布策略/说明测试通过。

重建后 APK 整体哈希不同。1665 项解包对比仅 `resources.arsc` 不同；aapt 资源值比较确认唯一差异是 `react_native_dev_server_ip` 从构建机 `10.1.0.157` 变为 `10.1.0.110`。因此不宣称 APK 字节级可重复；源码、DEX、JS、原生库和素材项一致。原始比较失败记录和两份资源导出均保留在 `evidence/development/`。

## 后续边界

用户已批准签名。`publish=false`，运行停在 `todo-moe-publish` 保护环境；未提交该环境审批，也未创建公开 Release。签名产物及校验记录已下载到项目内。

手机方便时再安装正式包，检查启动、导航、新增、完成/撤销，以及所需的 Dev 备份迁移。Stable 与 Dev 包和数据独立；同步继续关闭，不清除或卸载旧版。正式公开发布仍需用户批准。

加密正式密钥已生成并配置到保护环境；交付文件不包含密钥或口令。离线密钥备份未独立确认。当前记录保存在文档分支，待手机验收结果补齐后统一合并，避免为状态记录再次触发完整 APK 构建。
