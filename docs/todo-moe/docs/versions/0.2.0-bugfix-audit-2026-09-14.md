# 0.2.0 定向缺陷修复候选 · 2026-09-14

本记录对应 `Todo-Moe-0.2.0-Bug-Audit.md` 的代码核验与修复。起点 HEAD 为 `d54d8568927b5048ac0174405d897c093371efe8`；已用 `git merge-base --is-ancestor` 确认报告基线 `87ac108f1be634739d9200d863c8e734d88f74ec` 是该起点的祖先。本轮保留 vc39 图标及其后续历史，不回退已有成果。

## 已实现

- B01：任务行、全局搜索和收件箱处理的 Undo 回调返回真实业务 Promise；任务行的视觉 pending 在 `finally` 按操作 ID 结束，失败只报告一次。新增真实任务行挂接真实 `ToastProvider` 的集成测试，确认 Toast 在延迟 Promise 结束前保持已认领状态。
- B02：完成提示使用统一的 `TASK_COMPLETION_TOAST_KEY` 替换身份，只保留最新可撤销任务；错误提示双向保持优先，后来的完成提示不会反抢当前错误；同步记录最新 ID，React 提交前的旧按钮也不能执行过时操作。
- B03：设置首页搜索所在 ScrollView 使用 `keyboardShouldPersistTaps="handled"`，代码路径允许键盘打开时首次点击结果直接交给菜单项。
- B04：已在 GitHub 仓库 Settings → General → Features 启用 Issues；刷新后复核开关为启用状态，并实际打开 `/issues/new` 得到新建 Issue 表单，未提交测试 Issue。
- B05：About、帮助、隐私、许可浏览和错误文案接入当前语言；中文保留原文，其他现有语言确定回退英文；许可证法律文本不改写。
- R01：在 `zizhan` 的 Dev vc37 沙盒中复现“完成后立即滚动”：固定坐标的旧完成画面覆盖到新滚入任务。反馈宿主现在于手指开始/移动、下一次交互及宿主窗口重排时只撤下短暂 paint，不清除 Undo 操作身份；补充自动化测试锁定滚动、点击折叠前和尺寸变化边界。
- R02：当前源码已有 `0a1c9d0c` 的延迟 `onPressOut` 保护和回归测试，vc37 定向手势录制未观察到勾号/爆发被提前取消；未再叠加推测性动画改动。

## 验证证据

- 修复前定向闭环：同一命令稳定得到 5 failed / 82 passed，分别命中 B01、B02 两项策略、R01 和 R02。R01/R02 的首轮推测性改动曾撤回，待设备连接后再按真实结果处理。
- 第一阶段修复后相关路径：5 files / 167 tests 全部通过，覆盖 Toast、任务行、全局搜索、收件箱处理和 About 本地化。
- 提交前复审补齐真实 Provider 集成、错误反向优先级与几何失效边界后，高风险路径为 5 files / 92 tests 全部通过。
- `typecheck:mobile` 通过。
- `lint:mobile` 为 0 errors / 79 warnings；警告均为仓库既有类型，新增 About 测试沿用现有 mock 后置 import 风格产生 1 条 `import/first` 警告。
- 完整 mobile：288 files / 2857 tests 通过，2 个用例失败。真实安装包补丁夹具在并行负载下超过默认 5 秒，随后以 15 秒上限隔离运行 2.54 秒通过；快速捕获日期断言在宿主 `Asia/Shanghai` 下把本地零点误当 UTC 零点，隔离仍失败，显式 `TZ=UTC` 后通过。两项均与本轮产品路径无交集，但完整套件仍如实记为非全绿。

## 真机与外部状态证据

- ADB 设备 `153f8f46` / `zizhan` 已连接；设备仅安装 `io.github.xiaolexldw.todomoe.dev`，manifest 回读为 `0.2.0 (37)`。Stable 包未安装。
- R01、R02 均只使用应用自带沙盒虚构数据；退出沙盒后更改可丢弃，个人任务未参与测试。
- 原始录像与逐帧联络表保存在 `docs/todo-moe/evidence/0.2.0-bug-audit/`。这些录像证明 vc37 基线行为，不冒充当前未安装工作树的修复后结果。
- GitHub Issues 设置与 `/issues/new` 入口已在登录后的网页会话中实测；未创建垃圾 Issue。

## 候选与安装结果

- 实现已提交为 `9018580b69ddeb4aed6449c9009e77abb18af0fd`，从干净工作区构建 Dev vc40。APK 身份、哈希、证书与内嵌源码校验结果见 [vc40 独立记录](0.2.0-dev-vc40-2026-09-14.md)。
- vc40 已通过 `adb install -r` 从 Dev vc37 覆盖安装；设备回读版本正确且首次安装时间保持。R01 用相同手势复测通过，修复后录像保存在 `evidence/development/device-20260914-vc40-bug-audit/`。
- B03 已由用户在键盘打开时输入“同步”并首次点击结果，ADB 回读确认直接进入“同步”页面且键盘收起，真机路径通过。R02 没有把一次设备手势写成普遍证明。
- 未推送代码、未创建 PR、未签名或发布新的 Stable；公开 Stable vc37 保持不变。

## 下一验收

1. 继续观察 B01/B02 的真实日用失败路径；不要用普通成功操作冒充存储失败验证。
2. 保持 Stable 和公开 Release 不变，直到获得明确发布授权。
