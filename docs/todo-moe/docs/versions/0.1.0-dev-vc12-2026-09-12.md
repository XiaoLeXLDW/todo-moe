# Dev vc12：渠道切换的旧Widget注册清理 · 2026-09-12

状态：干净源码构建及APK工程核验通过，当前后续安装候选；尚未安装、未发布，不能据此放行完整设备矩阵。

源码`87cd7d55df676b1f61c1da98906f18e1e82d71ec`，dirty=false，0.1.0 / Dev / versionCode12，包名`io.github.xiaolexldw.todomoe.dev`。构建3m30，APK 42,003,696字节；SHA-256 `9d081ce4cb668208dda6c1e32f9b726cf59c216970d04153166b3bbf2df8bdd6`。源码ZIP SHA-256 `44d63ff32a7df9c988eff33715128cd68efc6fee99e884a5d0b8263d287b67fe`。仍为原Dev测试证书`fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c`。

vc11已验证精确intent-filter去重，但实际APK仍包含另一Todo Moe渠道的旧legacy Widget receiver。当前品牌最终处理只移除另一个已知渠道的精确`.widget.TasksWidget`注册；保留本渠道、相对类名、上游模块Provider和其他组件，也不删除生成Java源码。

真实android-widget插件循环与保留边界先新增3 RED（原5项仍通过），修复后相关9 GREEN、71次断言；完整工程124 pass/1默认跳过/0 fail，2748次断言。独立只读复核未发现阻断。固定提交实际完成六次Dev→Dev→Stable→Stable→Dev→Dev prebuild：每次本渠道legacy Widget恰好一个、另一渠道无残留、无重复filter/data；同渠道生成结果保持不变。原始日志/XML/判定在`evidence/development/intent-prebuild-87cd7d55/`。

实际APK核验通过：包名/源码/版本、签名、16KiB zipalign、32个ELF库对齐、Hermes诊断标记不存在（诊断vc9实际APK阳性对照通过）。二进制Manifest无重复入口、无另一Todo Moe渠道的组件注册；全部52个activity/alias/receiver/service/provider的类型和名称集合与干净CI样本`d02c44e7`完全一致。前一次vc11的同一APK断言明确失败，本次通过；这不是Widget实际渲染或通知到达的设备结论。

[本地产物与源码](../../../../artifacts/0.1.0-dev-vc12/README.md)包含原manifest、SHA256SUMS、编译Manifest判定、Widget渠道判定及组件集合对照。构建日志为`evidence/development/build-dev-vc12-87cd7d55.log`。原vc10/vc11缺陷样本保留且标明未接受为后续候选。

[根因与旧样本](../../NATIVE-INTENTS-20260912.md)。手机仍为vc7；诊断vc9待外屏解锁后复测，然后才能升级干净候选。完整设备矩阵、正式发布和7天日用仍未完成。
