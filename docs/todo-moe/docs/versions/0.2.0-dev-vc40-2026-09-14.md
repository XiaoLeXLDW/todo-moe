# 0.2.0 Dev vc40：定向缺陷修复候选

本候选落实 `Todo-Moe-0.2.0-Bug-Audit.md` 的 B01–B05，并在真机复现 R01 后补充最小范围修复。R02 当前源码已有延迟 `onPressOut` 保护；定向设备手势未复现风险，因此没有叠加推测性动画改动。

- 源码：`9018580b69ddeb4aed6449c9009e77abb18af0fd`，构建时工作区干净。
- 包名：`io.github.xiaolexldw.todomoe.dev`；版本 `0.2.0`，versionCode `40`。
- APK：`build/moe/development-40-9018580b69dd/todo-moe-0.2.0-development-vc40-app-release.apk`。
- 字节数：`42,496,084`。
- SHA-256：`14e34995e4e5544dc5b5610244f119ed92cefae466e672cd9b20babba13d5702`。
- Dev 证书 SHA-256：`fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c`。

## 验证

- 构建脚本回读实际 APK 的包名、versionName/versionCode、签名、内嵌配置与源码身份，全部通过；Gradle `BUILD SUCCESSFUL`，耗时 3 分 19 秒。
- 高风险定向集为 7 files / 190 tests 通过；子代理扩展复审为 8 files / 201 tests 通过；Mobile TypeScript 通过；Expo lint 为 0 errors / 79 个既有 warnings；README 对称检查和 `git diff --check` 通过。
- 完整 mobile 为 288 files / 2857 tests 通过、2 项非全绿：原生补丁夹具在并行负载下超过默认 5 秒，隔离 2.54 秒通过；快速捕获日期断言在上海时区失败，显式 `TZ=UTC` 后通过。两项均与本轮改动路径无交集，但不把全量套件写成全绿。

## 安装与真机

已校验哈希后以 `adb install -r` 从同包 Dev vc37 覆盖安装到 `zizhan`；安装成功，回读为 `0.2.0 (40)`，原 `firstInstallTime=2026-09-11 20:29:00` 保持，未通过卸载清数据绕过升级。

R01 在 vc37 沙盒录像中可见固定完成画面覆盖新滚入任务；vc40 以相同“完成后立即滚动”手势复测，滚动开始后旧画面立即撤下，未再覆盖新任务。修复后材料位于 `evidence/development/device-20260914-vc40-bug-audit/`。B03 真机搜索输入受当前手机输入法拒绝 ADB 文本注入，未伪称已完成该项真机点击；代码路径、自动化测试和类型检查通过。

## 发行边界

这是 Dev 测试包，不使用正式 Stable 私钥，未推送分支、未创建 PR、未替换公开 Release。Stable vc37 保持不变；后续公开发布仍需用户明确授权。
