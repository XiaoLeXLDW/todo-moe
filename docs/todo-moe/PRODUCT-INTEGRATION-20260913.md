# Todo Moe 产品整合 · 2026-09-13

当前基线：已发布 0.1.0 Stable vc33，main `235305f3`。用户指定对话最新消息为本轮目标，优先于此前“已接受/已完成”记录。沿用正式身份/密钥及已工作的 SukiSU 玻璃，不覆盖 vc33 APK。

## 本轮任务

1. 唯一设置首页：同步状态和备份一级可见；外观与动画拆成独立页面；复用现有设置路由，不新增第二套总目录。
2. 删除家族色，明暗模式为系统/浅/深，默认系统动态色并提供自定义主色、预设与 HEX；RN 与 Compose 同源语义色；旧偏好迁移保留数据、触感及用户明确的减弱动画选择。
3. 明显增强完成反馈及清单庆祝，统一真实绘制参数，增加不写入任务库的即时预览；立即写业务、快速撤销取消旧效果，装饰不截获点击。
4. 保留猫咪家族形象，移除图标内嵌底板，透明前景与全幅背景分层，检查真实 Alpha 和最终 APK/Launcher。
5. 自有 About/帮助/隐私/README/GitHub 产品入口；移除上游视频和营销，构建信息折叠，保留来源/版权/许可证及 FOSS 边界。
6. AGPL 全文打包、可离线阅读，核对实际复制代码和随包第三方通知；给旧 vc33 补充许可材料，不替换旧 APK，不宣称法律全面认证。
7. 沿用正式密钥，以新版本候选交付；仅针对实际改动验证，最后集中验收设置、颜色、强动画、图标与关于，不逐卡构建或要求手机操作。

## 执行与证据

root：设置/偏好/动态配色及整合；内置 default 子代理分别处理动画、产品资料/许可、图标。各写入范围隔离。当前工作分支 `feat/product-integration`。

系统动态色仅读取 Android 系统调色板，不读取壁纸图片；API不支持时明确回退。正式版从空白开始、同步不启用，保留 Dev，不迁移个人任务。新版本公开发布仍需相应授权。

图标生成首次两轮均输出无 Alpha 的棋盘格，已拒收且保留证据；程序轮廓提取方式等待用户选择，不以假透明交付。其余任务持续推进。

## 当前进度（整合提交前）

设置首页及旧入口已统一；系统调色板桥接、明暗/自定义主色与偏好迁移已实现。颜色使用同一语义映射传给 RN 和 Compose，原生 Kotlin 编译成功。增强反馈与清单庆祝真实路径/预览已共享渲染器，撤销及生命周期门禁保留。

27 个相关测试文件/180 项测试通过；之后配色生命周期3项、设置与许可6项、共享庆祝相关项通过。最终 TypeScript 检查通过。自定义颜色针对背景、卡片、输入区及主色按钮文字校验对比度。尚未构建最终候选，不能据此宣布本轮观感通过。

vc33 已增加 `LICENSE-AGPL-3.0.txt`，内容逐字节来自批准源码 c334a173 的 LICENSE，SHA-256 `3e44a5b2e4d426157944940fec9e450e2c0f202d4ad5bc475844e05b26d534eb`；原四项签名材料和 APK 哈希未变。发布校验只容许这个来自批准源码的精确许可附件，不接受任意新文件；50 项相关测试和真实只读已发布校验通过。GitHub Description/主页已切换到自家产品与 Releases。

当前唯一需要用户输入的事项：是否允许在图像生成无法给出 Alpha 后，用程序按主体轮廓提取透明前景。图标完成后再构建 0.2.0 的单一候选，集中验收。


用户已允许程序抠图。图标透明提取、浅深圆形/squircle预览及安全区检查已通过；原候选421098个深色像素零删除、零RGB改变。新前景888212个透明像素，轮廓最大半径310.501px，小于安全半径312.889px。正式素材标记 family-mascot-checklist-v2-alpha；Launcher 和本轮整体视觉仍待集中验收。
